import styled from 'styled-components';

const Label = styled.label`
	display: block;
	margin-top: 2vh;
`;

const Checkbox = styled.input``;

const TableUI = styled.table`
	width: 100%;
	margin-bottom: 3vh;
`;

const Head = styled.thead`font-weight: bold;`;

const Row = styled.tr``;

const Column = styled.td``;

const Body = styled.tbody``;

export { Label, Checkbox, TableUI, Head, Row, Column, Body };
